{% extends 'base.html.twig' %}

{% block title %}Repas{% endblock %}

{% block body %}
    <h1>Repas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repa.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ repa.date ? repa.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Periode</th>
                <td>{{ repa.periode }}</td>
            </tr>
            <tr>
                <th>Valide</th>
                <td>{{ repa.valide ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repas_index') }}">back to list</a>

    <a href="{{ path('app_repas_edit', {'id': repa.id}) }}">edit</a>

    {{ include('repas/_delete_form.html.twig') }}
{% endblock %}
