{% extends 'base.html.twig' %}

{% block title %}Repas index{% endblock %}

{% block body %}
    <h1>Repas index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Periode</th>
                <th>Valide</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for repa in repas %}
            <tr>
                <td>{{ repa.id }}</td>
                <td>{{ repa.date ? repa.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ repa.periode }}</td>
                <td>{{ repa.valide ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_repas_show', {'id': repa.id}) }}">show</a>
                    <a href="{{ path('app_repas_edit', {'id': repa.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_repas_new') }}">Create new</a>
{% endblock %}
